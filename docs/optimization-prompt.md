# 后续优化用 Codex 指令

以下内容可以直接交给 Codex：

```text
请继续优化 EpochFlow（纪流），一个用于学习、调试和展示 Agent 技术的工作台。

唯一允许修改的项目目录是：
D:\25364\桌面\大模型Agent\EchoMind\Epochflow

原 EchoMind所有代码+详细文档+简历_hzq 和同级数字教师项目保持不变。Epochflow 已从现有 Vue 3 + Vite、Python + FastAPI 代码隔离复制，已提供免费模式、render.yaml、部署文档和基础验证。

先阅读本项目 README.md、docs/render-free.md、适用的 AGENTS.md 和当前代码，然后实际完成优化，不只输出方案。

1. 重设计前端：中文为主，浅色中性背景、清晰字体与留白、蓝绿色强调色。采用侧边导航和主工作区，完善对话、知识库、评测、执行详情及设置页面。拆分大型 App.vue，统一组件样式与 API 请求逻辑，适配手机并验证可访问性。

2. 保留真实 Agent 执行链路，展示结构化的意图识别、路由、工具调用和检索结果；不能使用模拟数据冒充真实响应，不展示模型私有思维链。所需功能没有接口时补充最小后端支持。

3. 整理后端路由、配置与业务模块，保持适合学习的结构，避免不必要的微服务化。完善统一错误、request_id、日志脱敏、异步资源关闭和前后端联调。

4. 严格保持免费部署方案：Render Python Web Service 使用 plan: free，前端使用免费 Static Site；不添加持久磁盘、付费数据库或其他付费资源。默认 STORAGE_MODE=memory，不启动本地 Docker。会话和上传知识属于临时数据，界面清楚说明休眠、重启、重新部署后会丢失。预置学习资料可由仓库内的脱敏文件恢复。

5. 免费模式使用有上限的内存会话与字符 n-gram 文本检索，不宣传为 Chroma 向量检索或长期记忆。可以改善轻量检索质量，但不能默认增加大型模型下载或外部付费依赖。完整模式的原数据库代码保留供以后学习。

6. 模型密钥只由后端读取，可在 Render Environment 设置；前端只能填写工作台访问码。未配置模型时保持可启动且明确提示，不伪造回复。模型 API 费用不属于 Render 免费实例额度，真实验证仅使用最小必要请求，不运行批量付费评测。

7. 新项目的启动与关闭脚本必须依据项目实际路径和进程身份操作，不能按 5173 或 8002 端口直接结束其他项目。不要删除原数据库、虚拟环境或模型缓存。复制来的旧脚本及 Docker 配置应安全整理为历史资料，或更新为可验证的新入口。

8. 完成相关测试、前端构建、桌面与手机页面检查和部署配置验证，修复发现的问题。无外部密钥时使用隔离的测试替身，并如实说明真实模型链路没有验证。补充简明的中文架构图和使用说明。

9. 准备上传到 GitHub 账号 ASEpochs 的新私有仓库 epochflow，只上传 Epochflow 目录。检查 GitHub 授权和仓库是否存在，不覆盖已有仓库，不复制原仓库历史，不上传密钥、依赖、数据库或缓存。有正确账号授权时创建并上传；缺少授权时先完成所有本地工作再说明缺少的登录步骤。

10. 更新 Render 免费部署指南，核实当前官方限制，说明同一 workspace 的免费服务共享额度。未经我明确确认，不创建付费资源或发布公网服务。

保留已有修改，持续执行到代码、验证和上传准备完成。常规实现选择自行判断，不在每个阶段重复询问是否继续。最终说明实际完成的改动、验证结果、GitHub 状态以及仍需我处理的事项。
```
